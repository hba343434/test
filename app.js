$(document).ready(function() {
    let box = $('.box');
    let icon = $('.fab.fa-telegram');
    
    if( box.hover() ) {
      // icon.removeClass('fa-telegram').addClass('fa-telegram-plane');
    }
    
    
  });